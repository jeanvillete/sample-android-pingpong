<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- argb -> alpha, red, green, blue -->
    <solid
            android:color="@color/background_home"/>

    <corners
            android:radius="@dimen/radius_input"/>

    <padding
            android:left="@dimen/padding_vertical"
            android:right="@dimen/padding_vertical"
            android:top="@dimen/padding_horizontal"
            android:bottom="@dimen/padding_horizontal"/>

</shape>